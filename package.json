{
  "scripts": {
    "build": "ncc build notify-slack/entrypoint.js -o notify-slack/dist"
  },
  "dependencies": {
    "@vercel/ncc": "^0.38.1",
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  }
}
