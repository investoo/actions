# action.yml
name: 'Run Tests'
inputs:
  NEEDS_DOCKER:
    description: 'Do tests require docker?'
    default: false

runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.NEEDS_DOCKER }}